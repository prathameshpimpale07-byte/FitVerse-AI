{
  "name": "fitverse-ai",
  "version": "1.0.0",
  "description": "FitVerse AI Fullstack MERN Application Root",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install",
    "start": "cd backend && node server.js"
  }
}
